[project]
name = "invoke-stubs"
version = "0.1.0"
description = "Partial PEP 561 type stubs for invoke: a @task decorator that keeps the decorated function's signature."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.11"
classifiers = ["Typing :: Stubs Only"]

[project.urls]
Homepage = "https://github.com/TheodoreAD/invoke-stubs"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["invoke-stubs"]
